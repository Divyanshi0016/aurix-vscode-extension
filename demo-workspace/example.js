// Sample file for testing AURIX scans.
// Replace this folder's contents with a real project, or point
// launch.json at a different path, once you're ready to scan
// something real.

function greet(name) {
  console.log("Hello, " + name);
}

module.exports = { greet };
